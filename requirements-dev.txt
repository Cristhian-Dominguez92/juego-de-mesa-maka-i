# Dependencias solo para desarrollo en escritorio. NO se empaquetan en el APK.
-r requirements.txt

# Audio en escritorio. pygame no está disponible en Android: el juego lo importa
# dentro de un try/except y funciona sin él. Se elimina en la Fase 3, cuando el
# audio migre a ft.Audio (incluido en flet 0.28.3, no requiere paquete extra).
pygame==2.6.1

# Herramientas de calidad de código.
ruff==0.16.1
pytest==9.1.1
