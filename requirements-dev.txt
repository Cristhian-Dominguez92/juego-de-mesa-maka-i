# Dependencias solo para desarrollo en escritorio. NO se empaquetan en el APK.
-r requirements.txt

# El paquete `flet` a secas no trae el cliente nativo: sin este extra,
# `python main.py` falla al intentar abrir la ventana. No hace falta para
# `flet build apk` ni para correr los tests.
flet[desktop]==0.28.3

# Herramientas de calidad de código.
ruff==0.16.1
pytest==9.1.1

# Solo para tools/optimizar_imagenes.py, que convierte los escaneos de la
# baraja a WebP. No se empaqueta en el APK ni hace falta para jugar.
pillow==12.3.0
