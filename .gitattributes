# Normaliza los finales de línea: LF en el repositorio, nativo en el working tree.
# Evita diffs espurios entre Windows (desarrollo) y Linux (CI de GitHub Actions).
* text=auto eol=lf

# Binarios: nunca tocar.
*.jpeg binary
*.jpg  binary
*.png  binary
*.mp3  binary
*.wav  binary
*.apk  binary
